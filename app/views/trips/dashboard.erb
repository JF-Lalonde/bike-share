
<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="dash-style.css"/>
    <title>Trips Dashboard</title>
  </head>
  <body>
    <h1>Trips Analytics</h1>
    <body>
      <a href="/trips">Trips</a>
      <h2>Average duration of a ride:</h2><p> <%= Trip.average_duration_of_a_ride %></p>
      <h2>Longest Ride:</h2><p><%= Trip.longest_ride %></p>
      <h2>Shortest Ride:</h2><p><%= Trip.shortest_ride %></p>
      <h2>Station with the most rides as a starting place</h2><p><%= Trip.station_with_most_start_trips.name %></p>
      <h2>Station with the most rides as an ending place</h2><p><%= Trip.station_with_most_end_trips.name %></p>
      <h2>Rides Breakdown 2013</h2>
      <h4>January</h4><%= Trip.monthly_rides_breakdown(1, 2013) %>
      <h4>Febuary</h4><%= Trip.monthly_rides_breakdown(2, 2013) %>
      <h4>March</h4><%= Trip.monthly_rides_breakdown(3, 2013) %>
      <h4>April</h4><%= Trip.monthly_rides_breakdown(4, 2013) %>
      <h4>May</h4><%= Trip.monthly_rides_breakdown(5, 2013) %>
      <h4>June</h4><%= Trip.monthly_rides_breakdown(6, 2013) %>
      <h4>July</h4><%= Trip.monthly_rides_breakdown(7, 2013) %>
      <h4>August</h4><%= Trip.monthly_rides_breakdown(8, 2013) %>
      <h4>September</h4><%= Trip.monthly_rides_breakdown(9, 2013) %>
      <h4>October</h4><%= Trip.monthly_rides_breakdown(10, 2013) %>
      <h4>November</h4><%= Trip.monthly_rides_breakdown(11, 2013) %>
      <h4>December</h4><%= Trip.monthly_rides_breakdown(12, 2013) %>
      <h4>Yearly Breakdown</h2><%= Trip.yearly_rides_breakdown(2013) %>
      <h2>Year Total 2014</h2>
      <h4>January</h4><%= Trip.monthly_rides_breakdown(1, 2014) %>
      <h4>Febuary</h4><%= Trip.monthly_rides_breakdown(2, 2014) %>
      <h4>March</h4><%= Trip.monthly_rides_breakdown(3, 2014) %>
      <h4>April</h4><%= Trip.monthly_rides_breakdown(4, 2014) %>
      <h4>May</h4><%= Trip.monthly_rides_breakdown(5, 2014) %>
      <h4>June</h4><%= Trip.monthly_rides_breakdown(6, 2014) %>
      <h4>July</h4><%= Trip.monthly_rides_breakdown(7, 2014) %>
      <h4>August</h4><%= Trip.monthly_rides_breakdown(8, 2014) %>
      <h4>September</h4><%= Trip.monthly_rides_breakdown(9, 2014) %>
      <h4>October</h4><%= Trip.monthly_rides_breakdown(10, 2014) %>
      <h4>November</h4><%= Trip.monthly_rides_breakdown(11, 2014) %>
      <h4>December</h4><%= Trip.monthly_rides_breakdown(12, 2014) %>
      <h4>Year Total 2014</h2><%= Trip.yearly_rides_breakdown(2014) %>
      <h2>Year Total 2015</h2>
      <h4>January</h4><%= Trip.monthly_rides_breakdown(1, 2015) %>
      <h4>Febuary</h4><%= Trip.monthly_rides_breakdown(2, 2015) %>
      <h4>March</h4><%= Trip.monthly_rides_breakdown(3, 2015) %>
      <h4>April</h4><%= Trip.monthly_rides_breakdown(4, 2015) %>
      <h4>May</h4><%= Trip.monthly_rides_breakdown(5, 2015) %>
      <h4>June</h4><%= Trip.monthly_rides_breakdown(6, 2015) %>
      <h4>July</h4><%= Trip.monthly_rides_breakdown(7, 2015) %>
      <h4>August</h4><%= Trip.monthly_rides_breakdown(8, 2015) %>
      <h4>September</h4><%= Trip.monthly_rides_breakdown(9, 2015) %>
      <h4>October</h4><%= Trip.monthly_rides_breakdown(10, 2015) %>
      <h4>November</h4><%= Trip.monthly_rides_breakdown(11, 2015) %>
      <h4>December</h4><%= Trip.monthly_rides_breakdown(12, 2015) %>
      <h4>Year Total 2015</h2><%= Trip.yearly_rides_breakdown(2015) %>
      <h2>Most ridden bike with total number of rides for that bike</h2><p>Most ridden bike:
      <%= Trip.most_ridden_bike.keys[0] %></p><p>Times ridden: <%= Trip.most_ridden_bike.values[0] %></p>
      <h2>Least ridden bike with total number of rides for that bike</h2><p>Most ridden bike:
      <%= Trip.least_ridden_bike.keys[0] %></p><p>Times ridden: <%= Trip.least_ridden_bike.values[0] %></p>
      <h2>User subscription type</h2><p><%= Trip.sub_type_breakdown %></p>

      ####Need to add trips sub ptags
      
      <h2>Single Date with highest amount of trips</h2><p><%= Trip.most_popular_date %></p>
      <h2>Single Date with lowest amount of trips</h2><p><%= Trip.least_popular_date %></p>
    </body>
  </html>
